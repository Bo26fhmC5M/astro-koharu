import { defineLive2DConfig } from './define-config';

/**
 * Live2D settings for this site. Keep the object empty to use every default.
 * Add only values that should differ from the public widget defaults.
 */
export const live2dConfig = defineLive2DConfig({});
